# 2026-04-25 — Bundle 1 cleanup: stop-the-bleeding pass

**TL;DR.** Multi-round repo audit (4 opus reviewers + 3 verifiers + 2 strategy reviewers) surfaced ~30 distinct findings; landed Bundle 1 covering active test failures, my own regressions from prior cleanup commits, public-API surface, and PushT undeclared deps. Single commit `da668e2`, 16 files changed, 1145+/35-. Drift tests 219/219 ✓. Branch 22 ahead of origin. Bundles 2-5 (doc parity for ContractionPPO/FlashSAC, release mechanics, CI, architecture cleanup) queued — see `.context/tmp/audit_2026-04-19.md`-era reports + final TODO synthesis in conversation.

---

## What landed

### Active test failures (silent or red)
- `tests/test_go2_warp_env.py` — assertions hardcoded `(51,)/(125,)/45` obs dims; real env returns `(48,)/(122,)`. Test was RED. Updated to real dims.
- `tests/test_determinism.py` — zero `assert` statements, only `print`s. Plus ran 50-iter PPO 3× as silent no-op (always passed regardless of outcome). Moved to `scripts/diagnostics/check_determinism.py` — research signal, not a pytest gate.
- `tools/bongo_board_test.py` — pytest collected 4 broken "tests" (CLI scenario fns named `test_drop`/`test_tilt`/etc., not pytest tests). Fixed by adding `testpaths = ["tests", "deploy"]` to `pyproject.toml`. The file no longer needs renaming.
- `deploy/test_policy_runner.py:21-23` — silently passed when no checkpoint found via `print + return`. Now uses `pytest.skip()`.
- `docs/api/pusht.md:235` — Python fence inside `!!! tip` admonition couldn't compile (4-space indent inside admonition body). Tagged ` ```python {.no-test}` to skip drift compile-check (signature is documentary, not a runnable example).

### Regressions from earlier `scripts/` move (commit `548889a`)
- `AGENT_HANDOFF.md:174` — codebase tree said `archive/train_offpolicy.py`; actual `scripts/archive/train_offpolicy.py` after the move. Fixed.
- `AGENT_HANDOFF.md:184` — said `tools/brax_baselines/`; actual `tools/archive/brax_baselines/`. Fixed.
- `NEW_AGENT_PROMPT.md:34` — "Root train scripts" → "Train scripts (in `scripts/`)". Verification question 1 ("the 5 root-level Python scripts") rewritten to match new layout.
- Working-tree `archive/` half-rename — `archive/{live_viewer,record_video_cpu,train_offpolicy}.py` were deleted in working tree but still in git index after the prior commit; `scripts/archive/` had untracked copies. Reconciled via `git rm` + stage. Git correctly recorded as renames.

### Public-API surface
- `jax_rl/algos/__init__.py` exports now include `FlashSAC` (was missing despite being a benchmark hero with own train script + tests + lessons).
- `jax_rl/configs/__init__.py` exports `FlashSACConfig` + `get_flash_sac_preset` (consistent with the SAC/TD3/Fast* exports).
- `jax_rl/utils/__init__.py` created (empty docstring + module marker). Was a PEP 420 implicit namespace package; would silently get dropped from any future wheel build.

### PushT undeclared transitive deps
- Cold reviewer caught: `jax_rl/envs/manipulation/pusht/pusht.py` imports `gymnasium`, `pymunk`, `pymunk.pygame_util`, `cv2`, `shapely`. **None of these were in `pyproject.toml`.** They were silently pulled in by an undeclared transitive `gym-pusht==0.1.6`. A clean `uv sync` with no extras would produce a venv where `from jax_rl.envs.manipulation.pusht import PushTEnv` fails.
- Added `[project.optional-dependencies]`:
  - `pusht = [gymnasium, pymunk, pygame, opencv-python, shapely]` — runtime deps for PushTEnv
  - `pusht-demos = [gym-pusht, datasets, pillow]` — for `replay.py` + `download.py`
- Reproduce cleanly with `uv sync --extra pusht`.

### Drift gate broadened
- `tests/test_docs_drift.py::test_no_reverted_obs_dims` previously only scanned `docs/` + `.context/` markdown. Now also scans `tests/*.py`. Excludes the test file itself (which contains the patterns by definition). Would auto-catch the `test_go2_warp_env.py` 51/125 drift class going forward.

---

## How we got here

Today's actions were the tail of a ~3-day audit cycle:

1. **Original audit** (`.context/tmp/audit_2026-04-19.md`) — 4-pass review of doc/code drift. 30+ findings, mostly addressed in commits `244219f`, `e3dac77`, `084b661` last week.
2. **Site disparity audit** (`.context/tmp/site_audit_2026-04-20.md`) — found PushT discoverability gaps. Addressed in `8b7bcd1`.
3. **Today's deep-stack audit** — 4 parallel opus reviewers (architecture, tests, docs, build), 3 verifiers (cross-checked findings), 2 strategy reviewers (audited the resulting TODO list itself + cold-independent triage).
4. **Bundle 1 = "stop the bleeding"** — surgical subset that has zero owner-decision dependencies and high confidence.

Rounds of verification caught real surprises:
- `dem` config field IS read at `gaussian.py:56`, NOT dead — but every actor unpacks 2-tuple while head emits 3-tuple when True. Setting `dem=True` would crash runtime.
- `test_go2_bongo_env.py` does NOT have stale dims (originally suspected, REFUTED — its 46/42/98/94 asserts match real env).
- `context_dim` + `context_fusion` in `networks_config.py` look dead but are RESERVED for Phase 6 DIAYN per `TODO.md:381`. Don't delete.
- PPO `encoder_config` in-place mutation has a latent aliasing bug when `critic_encoder is None` — both writes target same dataclass, first overwritten. Harmless today only because `MlpEncoder` ignores `obs_dim`.
- `gen_cli_reference.py` is 428 lines hand-mirroring argparse — will drift every CLI change. Refactor candidate for Bundle 5.

---

## What's still queued

Sequencing rationale: signal-first, then surface, then mechanics, then refactors.

- **Bundle 2** — public-site doc parity for ContractionPPO + FlashSAC; 4 plan files marked executed; stale `.context/sac_plan.md`-style citations cleaned up. ~2-3h. Default plan-archive convention: move to `.superpowers/plans/archive/`.
- **Bundle 3** — release mechanics (LICENSE, `[build-system]`, flax rev pin, real description). Only matters if pursuing pip-install. Default: clone-only, skip most of this except LICENSE + description.
- **Bundle 4** — CI workflow for non-slow tests, slow markers on mjx/Warp tests, CUDA OOM mitigation strategy. ~half day.
- **Bundle 5** — architecture cleanup (PPO aliasing fix, `_soft_update` extract, terrain logger move, distributional clamp dedup, generated XML cleanup). Opportunistic, chip away when touching adjacent code.

Owner-decisions still pending: plan-archive convention, LICENSE choice, pip-install target, CI scope, `dem` field fate, `tools/` triage. Defaults documented in conversation.

---

## Files touched (commit `da668e2`)

```
.context/AGENT_HANDOFF.md            (M, 2 path fixes)
.context/NEW_AGENT_PROMPT.md         (M, 2 phrasing fixes)
deploy/test_policy_runner.py         (M, pytest.skip)
docs/api/pusht.md                    (M, fence skip-marker)
jax_rl/algos/__init__.py             (M, FlashSAC export)
jax_rl/configs/__init__.py           (M, FlashSACConfig + preset export)
jax_rl/utils/__init__.py             (A, new package marker)
pyproject.toml                       (M, testpaths + pusht extras)
uv.lock                              (M, lockfile re-resolve from extras)
tests/test_docs_drift.py             (M, scan tests/*.py)
tests/test_go2_warp_env.py           (M, real obs dims)
tools/survey_artifacts.py            (M, 🎬 video flag — adjacent improvement)
archive/{live_viewer,record_video_cpu,train_offpolicy}.py  (R → scripts/archive/)
tests/test_determinism.py            (R → scripts/diagnostics/check_determinism.py)
```

## Verification post-merge

- Drift tests: `uv run python -m pytest tests/test_docs_drift.py tests/test_docs_code_blocks.py -q` → 219 passed, 44 skipped, 7 deselected
- Public smoke import: `from jax_rl.algos import PPO, PPOContraction, SAC, TD3, FastTD3, FastSAC, FlashSAC` ✓
- Config smoke import: all 8 configs + 6 preset getters resolve
- `archive/` directory removed; `scripts/archive/` has 3 legacy scripts; root has zero `.py` files
