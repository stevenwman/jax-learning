# 2026-03-24 — PPO Performance Investigation & Fast Training

## What we did

### Brax PPO A/B test — confirmed env is fine
Ran Brax's own PPO on both Go1 and Go2 at 512 envs, 50M steps:
- **Go1 Brax PPO:** eval 21.7 — matches the published Playground paper curve (~25 at 100M)
- **Go2 Brax PPO:** eval 17.9 — similar trajectory, slightly lower (different robot physics)
- **Our PPO Go2:** eval 9.0 — 2x worse sample efficiency

Conclusion: env produces correct rewards, our PPO implementation had real differences.

### Contact physics fix
Discovered Menagerie's Go2 MJCF has marshmallow-soft foot contacts (solimp=0.015) vs Playground's Go1 firm contacts (solimp=0.9). Fixed in `go2_base.py` by overriding foot geom properties after model loading.

### Wall-clock profiling — found 87x overhead
Python collect loop (per-step select_action + buffer.add + norm) forces GPU pipeline sync:
```
Pure env.step (512 envs, 20 steps): 0.111s → 92,000 sps
Python collect loop:                9.729s → 1,053 sps
```

### Built train_ppo_fast.py — lax.scan collect
Replaced Python collect loop with `jax.lax.scan`. Result: **55k sps** (up from 32k). Remaining gap to Brax's 82k is the update phase.

### Code-diffed Brax PPO vs ours — found 2 real differences
Instead of running more training, just read the code:

1. **Value loss scaling:** Brax `0.5 * 0.5 = 0.25x`, ours `1.0x` → 4x larger critic gradients
2. **Advantage normalization:** Brax full-batch, ours per-minibatch → higher variance

Fixed both in `ppo.py`. Running seed 600 to validate — early results show gap narrowing from 2x to ~1.3x.

### Debugging doc maintained
Full trail in `.context/go2_ppo_debugging.md` with 8 hypotheses tested, run log, and key lessons.

## Key files changed
- `jax_rl/algos/ppo.py` — vloss scaling 0.25x, full-batch adv normalization
- `jax_rl/envs/locomotion/go2_base.py` — firm foot contacts override
- `jax_rl/envs/locomotion/go2_joystick.py` — dict obs, linvel, privileged_state
- `train_ppo_fast.py` — NEW, lax.scan collect variant
- `train_brax_ppo_go1.py` — NEW, Brax baseline for Go1
- `train_brax_ppo_go2.py` — NEW, Brax baseline for Go2

### Go1 A/B validation — our PPO matches published results
Ran our fast PPO on Go1JoystickFlatTerrain (1024 envs, 50M steps, 110k sps):
- **Final eval: 27.4 ± 1.5** — matches the published Playground curve (~25-30 at convergence)
- Brax PPO on Go1 at 50M: 21.7. We beat Brax at the same step count.
- Video recorded — Go1 is walking, tracking velocity commands.

### Go2 fast PPO launched (seed 800)
1024 envs, 100M steps, 83k sps, full PG-matched config. At 16M steps: eval 9-14.5, oscillating. Early — Go1 didn't climb until ~30M.

### Live viewer built
`live_viewer.py` — WASD keyboard control via `mujoco.viewer.launch_passive()`. Needs local display (SSH won't work). Ready for when Steven is local.

### Commits (4)
- `e7fee9f` feat: add Go2 Joystick env
- `e2381a2` fix: PPO value loss 0.25x scaling, full-batch adv norm, dict obs support
- `5fd468d` feat: add train_ppo_fast.py (lax.scan collect) and Brax A/B test scripts
- `e3f88c5` docs: Go2 debugging trail, lessons, journals, plan updates

### Go2 100M step result — plateau at ~15
Seed 800 finished: **eval 9.8 ± 6.2**. Oscillated 8-18 for the entire run, never broke through. Video confirmed: robot shuffles, doesn't lift feet.

### Reward weight tuning — modest gains, not the fix
- Seed 900 (feet_clearance -2.0→-1.0): eval 16.1 at 20M (vs 13 baseline). Helped but didn't break through.
- Brax Go2 reward breakdown revealed: pose=445 (standing dominates), feet_clearance=-116 (biggest penalty), feet_air_time=-0.9 (feet never lift).

### Deep env audit — found real integration bugs (IN PROGRESS)
Commissioned thorough audit comparing Go2 env vs Go1 line-by-line. Found:
1. **Missing CCD iterations** — Go1 sets `ccd_iterations=20`, Go2 used default of 4. Fewer iterations = missed foot contacts = corrupted feet rewards. Fixed in go2_base.py.
2. **Incomplete privileged state** — Go2 critic got 116d, Go1 gets 122-130d (missing accelerometer, xfrc_applied). Critic had less info. Fixed in go2_joystick.py (now 122d).
3. Foot order difference (FL,FR,RL,RR vs FR,FL,RR,RL) — verified NOT a bug since rewards sum across all feet.

Smoke test running (seed 1000, 20M steps). Not yet validated — this is the hypothesis, not the conclusion.

### Key realization
We spent ~6 hours tuning reward weights when the env integration was the likely problem. The meta-lesson: when training plateaus, audit the env before tuning hyperparams.

## Status
- Seed 1000 running (CCD fix + privileged state fix, 20M steps)
- If it doesn't help, next steps: reward breakdown diagnostic in env, or deeper physics audit
- Go1 validated — PPO is correct, 27.4 matches published results
- TODO: online return tracker, record_video dict obs, best-policy checkpoint saving
- live_viewer.py ready but needs local display
