# 2026-03-22 — Infrastructure Cleanup + Q Diagnostics

## What we did

### FastDSAC Inf root cause found
Stress tested FastDSAC with edge case injection instead of waiting for full training runs. Found that MJX produces `Inf` (velocity overflow), not just `NaN` (solver failure). Our guard only checked `isnan()`. Single `Inf` obs → `Inf * 0 = NaN` in network → total param corruption.

Fix: `jnp.isinf()` added to env step guard alongside `jnp.isnan()`.

FastDSAC 1024 envs with both fixes (Inf guard + 400K buffer) running overnight.

### TrainConfig cleanup
Moved PPO-specific fields (`num_steps`, `policy_hidden_dim`, `value_hidden_dim`, `anneal_lr`, `max_grad_norm`, `squash`, `state_dependent_std`, `activation`) from TrainConfig into PPOConfig. TrainConfig now has only shared fields. Off-policy presets no longer need `ppo=None` or `anneal_lr=False`.

Code done, needs GPU test before commit (GPU occupied by FastDSAC).

### Checkpoint purge
Audited 87 checkpoints (624MB). Kept 13 reference benchmarks + 2 active runs. Purged 72 checkpoints, freed 475MB (624→149MB). Created `.context/benchmark_results.md` summarizing all key results with checkpoint paths.

### Q prediction diagnostics
Added optional `q_fn` argument to eval runner. When provided, computes:
- Q bias (mean(Q_pred - MC_return))
- Q RMSE
- Q-MC correlation
- Q mean vs MC mean

Backward compatible — existing train scripts work unchanged. To enable, pass a 3-line Q function closure at eval time. Needs GPU test.

### .context/ cleanup
- Moved `refactor_idea.md` to `executed/` (refactor complete)
- Updated `FAST_ALGOS_LIT_MISMATCH.md` — Paper 1 mismatches now show FIXED status
- Updated `AGENT_HANDOFF.md` — root-cause debugging preference, stress test workflow

### FastDSAC 1024-env: Inf guard CONFIRMED
FastDSAC survived **54.3M steps with zero NaN** — past the 53M crash point from the previous run. The `isinf()` guard was the fix. Training oscillates heavily (eval 58-282) but is numerically stable.

### Go2 Sim-to-Real Plan
Comprehensive plan written in `.context/go2_sim_to_real_plan.md` after dispatching 5 research agents:
- Go2 frameworks (RSL-RL, legged_gym, walk-these-ways)
- quad-sdk (not suitable — Spirit 40 only)
- Unitree SDK2 deployment pipeline
- unitree-go2-mjx-rl repo (env structure, obs/action details)
- MJLab (actuator models, composable reward pattern)

Key decisions: MjxEnv subclass, 31-dim obs × frame stack, legged_gym rewards, Kp=20/Kd=0.5 (Unitree official), PPO first then SAC, domain rand as robot-agnostic wrapper.

## Pending (needs GPU)
- [ ] TrainConfig cleanup commit (test suite)
- [ ] Q diagnostics wiring into train scripts
