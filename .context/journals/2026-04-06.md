# 2026-04-06 — Repo Audit & Cleanup

## What happened
Dispatched 4 parallel audit agents across the entire codebase:
1. **Env code** — dead code, consistency, readability
2. **Training/algos** — interface patterns, duplication
3. **Docs** — staleness, cross-references
4. **Utils/configs/tests** — coverage, deps, file organization

Overall scores: Envs 7.5/10, Training/Algos 8.5/10, Docs 9/10, Utils/Configs 8/10.

## Changes made

### Code quality
- **Algo docstrings** — TD3, PPO, FastTD3, FastSAC now have closure-pattern docstrings matching SAC's gold standard
- **Env obs dim** — go2_joystick.py header fixed from `~100d` → `122d` (verified by counting ObsTerms)
- **Replay buffer** — `_extra_next_keys` was recomputed via dict comprehension every loop iteration; now built incrementally. 6 new tests for extra obs (asymmetric critic) added.
- **Inline imports** — `import numpy as _np` (×5 across go2_base, go2_warp_base) and `import math` (fast_sac) moved to module level
- **Dead code annotations** — `_torso_mass`, `_feet_geom_id`, `_floor_geom_id`, `last_last_act`, `metrics` param all marked with `# UNUSED` + rationale
- **Archive dirs deleted** — `jax_rl/buffers/archive/` (old numpy buffer) and `jax_rl/networks/heads/archive/` (GaussianQHead for nonexistent FastDSAC). Recoverable from git.

### Config & presets
- **Go2WarpJoystickFlat presets** — added to both PPO `PRESETS` and `FAST_SAC_PRESETS` with proven HPs (eval 276.5 reference)
- **obs_normalization** — FastSAC/FastTD3 configs: added comment explaining why we default False (paper uses True on DM Control, untested on Go2). Added A/B ablation to TODO.

### Docs
- **TODO.md** — removed stale test counts (125/137/197) from completed items, added obs_norm ablation task
- **AGENT_HANDOFF.md** — test count updated to 221

## Audit items deferred
- MJX joystick env may be deprecated (Warp is preferred) — skip shared reward extraction (#6)
- go2_cpu.py physics duplication (#1) — same MJX deprecation question
- Magic numbers (#7) — already commented inline, not worth extracting
- Tests for live_viewer.py, record_video_cpu.py (#10) — peripheral scripts, low priority
- Dead code deletion (#2) — annotated instead, delete in future cleanup pass

## Documentation site

Set up MkDocs + Material theme docs site with 20 pages:
- **Getting Started:** installation, quickstart (CartpoleBalance in 5 min), key concepts
- **Tutorials:** Go2 locomotion, custom env (bongo board example), custom rewards (RewardSpec/ObsSpec), sim-to-real pipeline, asymmetric critic
- **API Reference:** 6 pages with mkdocstrings autodoc from source docstrings (algos, envs, configs, buffers, wrappers, networks)
- **Reference:** CLI flags, env presets, architecture (Mermaid diagram), lessons learned
- **Contributing:** dev setup, tests, code style

Infrastructure:
- `mkdocs.yml` — Material theme, dark/light toggle, nav tabs, search, code copy, Mermaid diagrams
- Embedded training videos (Go2 walking, bongo board handstand) on landing page + tutorials
- Auto-generators: `docs/scripts/gen_cli_reference.py` and `docs/scripts/gen_env_presets.py` — regenerate CLI flags and preset tables from actual code
- GH Actions workflow (`.github/workflows/docs.yml`) created but dormant until repo exists
- Superpowers specs/plans moved from `docs/superpowers/` to `.superpowers/` to avoid overloading `docs/` directory
- Context doc at `.context/docs_site.md` for future agents
- CLAUDE.md updated with doc sync rules for training scripts → generators

## Test results
- 221 tests collected (215 prior + 6 new buffer tests)
- All pass on CPU (`JAX_PLATFORMS=cpu`)
- GPU-only tests (Warp env, DM Control batched) expected to fail on CPU
