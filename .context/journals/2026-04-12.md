# 2026-04-12 — Off-Policy Loop Extraction

## What happened

Executed the off-policy loop extraction refactor planned on 2026-04-10. The 4 per-algo training scripts (`train_sac.py`, `train_td3.py`, `train_fast_sac.py`, `train_fast_td3.py`) now delegate to a shared `jax_rl/training/offpolicy_loop.py::run_offpolicy_loop` helper. Each script dropped from ~300 lines to ~115-130 total (~30-43 line `train()` bodies). FlashSAC stays standalone.

### Approach: shared function, not base class

The review (2026-04-10) initially recommended re-unifying the 4 scripts back into one `train_offpolicy.py`. Steven rejected this — the problem was signal-to-noise, not duplication. The fix was extracting the loop into a function that each thin per-script calls, keeping algo-specific decisions (optimizer, explore closure, log fields) visible at the top level.

### What was included

| Task | What | Commit |
|------|------|--------|
| Task 0 | Register `pytest.mark.slow` marker in `pyproject.toml` | `240a47f` |
| Task 1 | `EnvBundle` dataclass + `make_env_bundle` wrapper | `3e6d59d` |
| Task 2 | Fix `metrics_logger.py` fmt-string bug (ignored caller's format) | `1c846dd` |
| Task 3 | Add `critic_obs_dim` param to `ObsPipeline.make_buffer` (non-breaking) | `d94f101` |
| Task 4 | Create `run_offpolicy_loop` helper + CPU stub-env smoke test | `e01e0cd` |
| Tasks 5-8 | Thin all 4 per-algo scripts | `ea73130` `7eaa1a8` `3b00ec4` `84c4fb3` |
| Task 9 | Delete `ObsPipeline.make_buffer_with_critic` | `85fded9` |
| Task 10 | Update `AGENT_HANDOFF.md` | `890c907` |
| Archive | Move `train_offpolicy.py` to `archive/` | `f775131` |

### Review process

3 independent reviewers:
1. **Initial reviewer** — caught 9 issues (2 critical: wrong test attribute name, off-by-one line range). Fixed.
2. **Adversarial reviewer** — caught 4 more (pytest.mark.slow not registered, no CPU-only smoke test, redundant obs_dim re-extraction, import placement). Fixed.
3. **Final integration reviewer** — approved with follow-up. Flagged helper is 161 lines vs 150 target (acceptable — the 11-line banner block is explicitly readability, not complexity). No blocking issues.

### Smoke test results (GPU was partially busy, used 128 envs + 30% VRAM)

| Script | Env | Steps | Eval | Time |
|--------|-----|-------|------|------|
| `train_sac.py` | CheetahRun | 200k | 71.7 | 36s |
| `train_td3.py` | CheetahRun | 200k | 2.6 | 34s |
| `train_fast_sac.py` | CheetahRun | 200k | 203.5 | 118s |
| `train_fast_td3.py` | CheetahRun | 200k | 157.5 | 113s |

All 4 ran cleanly through the full loop: env → buffer warmup → gradient updates → logging → eval → checkpoint.

### Key design decisions

- **`make_env_bundle` wraps `make_envs`** instead of changing `make_envs` signature — avoided churning 11 existing callers
- **Non-breaking ObsPipeline merge** — added `critic_obs_dim` to `make_buffer` first, deleted old `make_buffer_with_critic` after all callers migrated (no broken intermediate commits)
- **Removed `_ts = training_state` cargo cult** — the lambda is called synchronously, so the Python closure capture concern was wrong. Replaced with a comment explaining why.
- **CPU stub-env test as commit gate** — runs in <12s, no GPU, catches glue bugs. Deferred real-env tests to GPU availability.

## Lessons

- **Extract loops, not classes** — when 4 scripts share 85% of code, the instinct is Trainer base class or re-unification. Both add noise. A shared helper function keeps each script readable while eliminating the duplication. The variation points (algo, optimizer, explore closure, log fields) are the function parameters, not virtual methods or config branches.
- **Non-breaking adds before breaking deletes** — extending a method's signature (add `critic_obs_dim` param with default `None`) then migrating callers then deleting the old method avoids broken intermediate commits. Two commits instead of one, but zero breakage window.
- **`pytest.mark.slow` requires registration** — unregistered markers don't skip tests, they just warn. Without `pyproject.toml` registration, "deferred" GPU tests run anyway and OOM on a busy GPU.

---

## Part 2 — Docs Site Audit + 4-Persona Review

After the refactor merged, did a full docs-site freshness sweep + dispatched 4 parallel Opus reviewers (high schooler, undergrad CS, PhD researcher, frontend engineer) using the pattern in `.context/references/docs_review_pattern.md`.

### Stale reference sweep (before reviewers)

Fixed 11 docs files with stale `train_offpolicy.py` commands (commit `ccd5439`), then updated obs dims + benchmarks to 51/125 + 285.1 (commits `80179b6`, `277f711`).

**Then caught a real bug:** the undergrad reviewer cross-referenced the `make_env_bundle` output against the actual env code and found the env produces 48d state / 122d privileged — not 51/125. The 51/125 claim came from AGENT_HANDOFF (written during a 2026-04-10 experiment that added linvel to state), but the change was reverted before merge to keep sim2real obs alignment. All my doc updates had been propagating a ghost — the docs said what AGENT_HANDOFF said, AGENT_HANDOFF said what an uncommitted branch said.

Reverted obs dims back to 48/122 across 10 files (commit `891ee78`). The benchmark table in AGENT_HANDOFF now notes the 285.1/280.1 results were trained on prior 51d obs space — **not reproducible** with current 48d env. Best reproducible result: 276.6.

### Reviewer findings (compiled from 4 personas)

Consensus praised: **Lessons Learned** (all 4 called it the most valuable page), **FAQ** (high schooler 5/5), **Glossary** (bridges JAX-from-PyTorch learners), **Custom Environment tutorial** (worked example with real code).

Critical issues found:
1. Obs dims 48/122 vs 51/125 (undergrad, PhD) — fixed in `891ee78`
2. ONNX export docs said "not yet implemented" but `jax_rl/utils/export.py` exists — fixed
3. FlashSAC tutorial suggested flags that don't exist on `train_flashsac.py` — fixed
4. Frame stacking claims contradicted themselves across pages — fixed
5. Jargon avalanche on index + concepts pages — softened (commit `360391d`)
6. Env presets table had 10 columns (unreadable on mobile) — split into `pymdownx.tabbed` tabs per algo (commit `2402105`)
7. Dead CSS for `mkdocstrings` classes — removed
8. No "last updated" timestamps — added `git-revision-date-localized` plugin
9. Single-seed benchmarks presented without qualification — added methodology note in `lessons-learned.md`

### Doc-site commits this session

| Commit | What |
|---|---|
| `ccd5439` | Replace archived `train_offpolicy.py` refs (11 files) |
| `80179b6` + `277f711` | Update obs dims to 51/125 (later reverted — premature) |
| `891ee78` | **Revert** obs dims back to 48/122, fix ONNX + FlashSAC + benchmark table |
| `360391d` | Single-seed benchmark note, jargon softening, frame-stacking cleanup |
| `2402105` | Presets table → tabs, dead CSS removed, git-revision-date plugin |

### Lessons from the docs audit

- **Docs that depend on uncommitted code are ghost refs.** AGENT_HANDOFF was updated in anticipation of a change that got reverted. Every downstream doc update I did was propagating stale info from that ghost. The 4-persona reviewer with code cross-reference caught it; the earlier reviewers trusted AGENT_HANDOFF as authoritative.
- **The undergrad cross-reference review caught what 2 other reviewers missed.** High schooler couldn't verify against code (lacks expertise). Frontend reviewer doesn't read algorithm code. PhD reviewer verified algo correctness but trusted the obs dim claims in the docs. Only the undergrad systematically checked "docs say X, code says Y" and found the gap.
- **Keep benchmark numbers tied to reproducibility status.** The 285.1 result is real but not reproducible with current code (51d obs reverted). Either keep 48d code and mark 285.1 as "frozen experiment" in the table, or commit the 51d code so 285.1 becomes reproducible. Doing neither means the number is advertised but untrustworthy.
- **`fetch-depth: 0` is required for `git-revision-date-localized`.** Shallow CI clones fall back to current time, silently breaking "last updated" timestamps. Document this in CI setup.

---

## Part 3 — Second 4-Persona Review + Truncation Bug Fix

After the first review round landed, dispatched a second 4-persona review to see what was still stale. New findings plus one big code bug.

### The truncation bug (critical)

PhD reviewer cross-referenced SAC/TD3 against Fast*/Flash* truncation handling and found they disagreed. Investigation confirmed:

- `EpisodeWrapper` sets `done = terminated OR truncated`, `truncation = truncated AND NOT terminated`
- SAC/TD3 (correct): `target = r + γ(1-done)V_next`, loss mask `(1-truncation)` — zeros bootstrap on both, drops pure-timeout rows from loss
- Fast*/Flash* (buggy): `effective_done = max(done, truncation)` (no-op — `done` already includes timeouts), no loss mask — pure-timeout rows contributed an `r`-only target, teaching `Q = r` at timeout steps

On long-horizon tasks (Go2, Humanoid) this caused systematic Q underestimation proportional to `timeout_rate × true_tail_value`. The FlashSAC docstring even said "terminated only" but the code passed `batch["done"]` which is term-OR-truncated — docstring was lying.

Fixed in commit `82c9fe5`. 3 algos now use the Brax convention (`target with done`, `loss with mask = 1-truncation`). 25 unit tests still pass. Actual training behavior on Go2/Humanoid should improve, especially the ceiling. Benchmarks from before this commit are not directly comparable.

Also discovered `handle_truncation` constructor arg on all 5 off-policy algos was dead code — stored on `self`, never read. The real switch is `cfg.handle_truncation` in the training loop (controls whether `truncation` gets populated in the buffer). Removed in commit `39c7ddc`.

### Other findings + fixes (round 2)

| Commit | Issue | Fix |
|---|---|---|
| `2c20f1f` | Pipe escape bug in `configs.md` — `tuple | None` broke table rendering | Escaped pipes in 14 rows |
| `2c20f1f` | ONNX export docs claimed "actor MLP (512-256-128)" — FastSAC/FastTD3 only, not FlashSAC | Labeled as FastSAC/FastTD3-only |
| `2c20f1f` | `AutoResetWrapper` `full_reset=False` default caches initial state — silent catastrophe without DR | Added warning admonition |
| `a7ec40c` | Frontend polish (10 items) | Sticky tabs, toc.follow, content.code.annotate, edit-this-page, palette media queries, validation block, code max-height, widened content, stronger zebra, MathJax dark fix |
| `1c13268` | Single-seed benchmarks cited as reliable | Caveats on index/algos/asymmetric-critic |
| `1c13268` | No end-to-end training loop example | New `docs/reference/training-loop.md` (~90-line annotated loop) |
| `1c13268` | Eval score scales unexplained (950 vs 270 vs 47) | One-line explainer tip on first eval mention in 3 tutorials |
| `1c13268` | Glossary buried under Reference nav | Promoted to top-level tab |
| `1c13268` | **18k sps RTX 4090 claim for Go2 was off by ~5x** | Real Go2WarpJoystickFlat FastSAC is 3.4-3.95k sps per journals. The 18k number was from CheetahRun. Fixed to "~3-4k sps" with a note that DM Control benchmarks run much faster. |

### Lessons from round 2

- **Ghost refs recurrence.** The 18k sps claim was the same pattern as the obs-dims bug: a number measured on one env (CheetahRun), cited generically on another (Go2). Not as egregious as the obs dims because the number was real, just mislabeled. But same failure mode — a later writer trusted the earlier writer who trusted a half-remembered measurement.
- **PhD cross-ref caught the truncation bug that 3 other reviewers missed.** High schooler couldn't read algo code. Frontend doesn't read algo code. Undergrad verified API surface matched code but didn't diff the algo internals against each other. Only the PhD reviewer systematically compared the 6 algos against each other and noticed the divergence. Reviewer diversity is the point.
- **Docstring audits.** FlashSAC's docstring said `done: terminated only` while `batch["done"]` was term-OR-truncated. Docstrings can lie silently for years. Worth periodic audits against actual wrapper semantics.
- **Validation blocks catch cross-agent drift.** Added `validation: links: unrecognized_links: warn` to mkdocs.yml; immediately caught a broken link between two parallel subagents (one created a link to a file the other was still writing). Would have shipped broken otherwise.
