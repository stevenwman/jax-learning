# 2026-03-18 — SAC Implementation & First Runs

## What we did

### SAC implementation completed
Built the full SAC algorithm from the plan doc (`.context/sac_plan.md`), matching MuJoCo Playground's Brax SAC reference config:

**New files:**
- `jax_rl/algos/sac.py` — TrainingState, critic/actor/alpha losses, Polyak target update, select_action
- `jax_rl/networks/heads/q_head.py` — QHead (concat(obs, action) → MLP w/ optional LayerNorm → scalar)
- `jax_rl/buffers/replay_buffer.py` — numpy circular FIFO replay buffer (add, add_batch, sample)
- `jax_rl/configs/sac_config.py` — SACConfig dataclass
- `train_sac.py` — full training script with checkpointing, metrics CSV, resume support

**Key design decisions (from plan):**
- Numpy replay buffer (not JAX) — keeps door open for non-jittable envs (Gymnasium, real robots)
- stop_grad on entropy term in alpha loss — JAX-specific, prevents gradient bleed into policy
- softplus + min_std for std parameterization (not exp(log_std)) — prevents policy collapse
- Twin Q with shared optimizer (single optax state over the (q1, q2) tuple)
- No obs normalization for SAC — Q-network LayerNorm handles input scaling

### Bug: obs normalization explodes off-policy replay

**Symptom:** Q-values diverged to -15,000, alpha to 19+, returns stuck at 32.

**Root cause:** Online obs normalization + off-policy replay buffer = catastrophic mismatch. At env reset, all 128 envs have near-identical observations → variance ≈ 0 for some dims → `normalize()` divides by `sqrt(0) + 1e-8` → stored `next_obs` values hit ±2 billion → TD targets at ±47 million on first gradient step.

**Why it only affects SAC, not PPO:** PPO is on-policy — obs are normalized and consumed in the same batch, so the normalization stats match the data. SAC's replay buffer stores normalized obs that become stale as norm_state evolves.

**Fix:** Removed obs normalization entirely for SAC. Matches Brax reference (which also doesn't normalize). The Q-network's LayerNorm provides implicit input normalization. Initialized norm_state as identity (mean=0, var=1) for checkpoint compatibility.

### SAC WalkerWalk — 975/1000 at 5M steps

| Metric | Value |
|--------|-------|
| Avg return (last 100 eps) | 975.0 |
| Max return | 995.1 |
| Min return (last 100) | 914 |
| Steps to convergence | ~800k (return >950) |
| Total steps | 5M |
| Speed | 9,618 sps |
| Q1 final | ~100 (stable, matches discounted return) |
| Entropy final | ~-3.0 (at target_entropy = -3.0) |
| Alpha final | 0.0014 |

For comparison: PPO took 60M steps to reach 833. SAC is 12x more sample efficient and achieves a much higher score.

### SAC HumanoidRun — 207 avg at 5M steps

| Metric | Value |
|--------|-------|
| Avg return (last 100 eps) | 207.2 |
| Max return | 234.7 |
| Min return (last 100) | 183 |
| Steps | 5M |
| Speed | 7,010 sps |
| Q1 final | ~15 (stable) |
| Entropy final | ~-10 (near target_entropy = -10.5) |

Matches MuJoCo Playground paper Figure 7 (~200 for SAC at 5M). PPO peaked at ~10 on this task at 60M steps.

## North star alignment check
Compared current repo state against `.context/rl_framework_plan.md`:
- Phase 1 (Foundation): ~90% complete. Missing: deterministic head, wandb logger
- Phase 2 (First Algorithms): PPO done (4 envs validated), SAC done. TD3 not started
- Architecture matches plan closely — encoder+head modularity, config dataclasses, functional TrainingState pattern
- Intentional deviations: numpy replay buffer (not JAX-native), no BaseAlgorithm ABC (premature), no Trainer class (train scripts are standalone)
- Notable gaps: no Wandb, no deterministic eval protocol, plan checklists are stale

## Lessons
- **Online obs normalization is incompatible with off-policy replay** — the near-zero variance at env reset produces enormous normalized values. Either normalize at sample time (complex) or skip normalization entirely (simpler, matches Brax SAC)
- **LayerNorm in Q-networks serves double duty** — stabilizes training AND implicitly handles input scaling, making explicit obs normalization unnecessary for off-policy methods
- **SAC's sample efficiency vs PPO is dramatic** — WalkerWalk: 975 at 5M vs 833 at 60M. HumanoidRun: 23+ at 1M vs ~10 at 60M. Off-policy + 8 gradient updates per step = serious leverage
