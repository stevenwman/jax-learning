# Docs Review Round 4 — Compiled Results

**Saved:** 2026-04-09 18:49:20
**Context:** After `train_offpolicy.py` was split into per-algo scripts (`train_sac.py`, `train_td3.py`, `train_fast_sac.py`, `train_fast_td3.py`). Another agent is concurrently refactoring the codebase, so this TODO may need re-validation.

## Score Progression

| Reviewer | R3 | R4 | Trend |
|----------|----|----|-------|
| High schooler | 3.1/5 | 3.2/5 | Flat |
| Undergrad | 3 errors | 8 pages affected by train_offpolicy split | Regressed due to refactor |
| PhD | 8/7.5/6 | 5/6/4 | **Dropped** — found runtime bugs |
| Frontend | 0 high issues | 3 high + 55 total findings | More polish issues surfaced |

## 🚨 Critical (actual runtime bugs, not docs)

PhD found these by running the code — these are real bugs in training/deploy code, not docs issues. **NOTE: another agent is refactoring concurrently — verify these still apply before fixing.**

1. **`train_offpolicy.py` crashes at first eval** — uses old `eval_runner` signature after the split refactor
2. **`train_flashsac.py` same bug** — also stale signature
3. **`deploy/sim2sim_direct.py` uses wrong PD gains** — imports `KP_SIM/KD_SIM` (archived MJX values) instead of `KP_WARP/KD_WARP`
4. **`--domain-rand` in legacy mode silently loads archived DR specs** — user gets old configs without knowing

## High-Confidence Issues (multiple reviewers)

| Issue | Reviewers |
|-------|-----------|
| `train_offpolicy.py` references stale in 8 pages | all 3 content |
| New per-algo scripts (`train_sac.py` etc.) not documented anywhere | all 3 content |
| `cli-flags.md` wrong defaults (`--log-interval`, `--eval-every`) | undergrad, PhD |
| Generator scripts `gen_cli_reference.py` / `gen_env_presets.py` stale | undergrad, PhD |
| MathJax won't re-typeset on `navigation.instant` switches | frontend |
| `.gl` underline invisible in dark mode | frontend |

## Docs-Only Quick Fixes

### Stale training script references (8 pages)

1. `concepts.md` — training scripts table
2. `cli-flags.md` — add 4 new script sections, fix defaults
3. `env-presets.md` — "Used by" lines
4. `architecture.md` — mermaid diagram + off-policy header missing FlashSAC
5. `train-locomotion.md` — step 3 command
6. `custom-env.md` — smoke test command
7. `sim2real.md` — step 2 command
8. `faq.md` — MUJOCO_GL example

### Generators

9. `gen_cli_reference.py` — add per-algo parser builders
10. `gen_env_presets.py` — update "used by" text

### Frontend polish (the biggest wins)

11. `mathjax.js` — add `document$.subscribe` for instant nav
12. `extra.css` — `.gl` dark mode bump, H2 section borders, display math spacing
13. `extra.css` — remove `display: block` on tables (Material already wraps)
14. `envs.md` — `??? note` → `???+ note` (open by default)

### Medium

15. `api/configs.md` — missing fields in SAC/TD3/FastTD3/PolicyHead (dem, obs_norm_eps, etc.)
16. `contributing.md` — test count `~241` is already updated, PhD wants it removed entirely
17. `glossary.md` — fix intra-page "see X" dead refs (auto-linker skips same-page)

## Specific factual errors to fix

| Page | Error | Correct value |
|------|-------|---------------|
| `cli-flags.md` | `train_ppo_fast.py` `--log-interval` default | `1` (not `10`) |
| `cli-flags.md` | `train_ppo_fast.py` `--eval-every` description | "default: 5000 episodes" (not 50000) |
| `cli-flags.md` | `record_video.py` `--seed` default | `0` (not `-`) |
| `cli-flags.md` | `train_offpolicy.py` missing `--reset-mode` flag | Present in argparse line 464-466 |
| `architecture.md` | Off-policy header missing FlashSAC | Add FlashSAC |
| `glossary.md` | "see Target entropy", "See Asymmetric critic" | Dead text (auto-linker skips same-page) |

## Frontend polish — top 10 from frontend reviewer

1. **[P1] Wire `document$.subscribe` into `mathjax.js`** so MathJax re-typesets after instant navigation
2. **[P5] Fix the glossary auto-linker to link intra-page references**
3. **[P13/P14] Remove `display: block; overflow-x: auto` on tables** — Material already wraps
4. **[P7] Bump `.gl` underline visibility in dark mode** — currently invisible
5. **[P16/P17] Add H2 section borders; remove redundant `---` hrs**
6. **[P21/P22/P23] Fix syntax highlight color collisions** (`.nv`/`.nf` duplication, low-contrast comments)
7. **[P11] Change reward-term admonitions from `??? note` to `???+ note`** — should be visible
8. **[P51/P52] Enable `mkdocs build --strict` in CI and add `validation:` block**
9. **[P34] Fix `.video-grid` mobile layout** (flex-basis, stack at 640px, `preload="metadata"`)
10. **[P31] Switch tutorials to `pymdownx.snippets` + code annotations** — biggest learning UX win

## Recommended order

1. **First: the runtime bugs** (verify still apply post-refactor)
2. Then: generators + all stale script references (one big commit)
3. Then: frontend quick wins (MathJax fix, `.gl` contrast, table display)
4. Then: medium docs fixes (missing config fields, `???+ note`, etc.)

## Files referenced by PhD reviewer

- `/home/stevenman/Desktop/Work/Research/jax-learning/train_offpolicy.py` (broken)
- `/home/stevenman/Desktop/Work/Research/jax-learning/train_flashsac.py` (broken)
- `/home/stevenman/Desktop/Work/Research/jax-learning/deploy/sim2sim_direct.py` (wrong gains)
- `/home/stevenman/Desktop/Work/Research/jax-learning/deploy/go2_constants.py` (constants OK, used wrong)
- `/home/stevenman/Desktop/Work/Research/jax-learning/jax_rl/training/eval_runner.py` (new signature)
- `/home/stevenman/Desktop/Work/Research/jax-learning/jax_rl/training/env_setup.py:107` (legacy DR fallback to archived module)
- `/home/stevenman/Desktop/Work/Research/jax-learning/docs/scripts/gen_cli_reference.py` (stale generator)
- `/home/stevenman/Desktop/Work/Research/jax-learning/docs/api/configs.md` (incomplete tables)
