[project]
name = "jax-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flax",
    "jax[cuda13]>=0.9.0",
]

[tool.uv.sources]
flax = { git = "https://github.com/google/flax.git" }
